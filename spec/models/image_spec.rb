require 'rails_helper'

describe "Image Model" do
  pending("something else getting finished") do
    raise "Item broken because of uninitialized constant Image::ImageUploader" do
    it {should validate_presence_of(:title)}
    end
  end
end
